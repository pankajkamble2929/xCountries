# 🌍 XCountries

A simple React app that shows country flags and names in a grid layout.

## 🚀 Features
- Fetches country data
- Shows flag and country name in boxes
- 7 boxes per row on desktop, 3 on mobile
- Light gray borders and equal image sizes
- Logs errors with: `Error fetching data: <error>`

## 🛠️ Setup
- npm install
- npm start
